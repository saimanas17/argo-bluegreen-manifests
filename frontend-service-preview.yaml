apiVersion: v1
kind: Service
metadata:
  name: frontend-service-preview
  namespace: bluegreen-demo
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
      nodePort: 30081  # Different port for preview/testing
  selector:
    app: nginx-bluegreen