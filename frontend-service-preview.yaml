apiVersion: v1
kind: Service
metadata:
  name: bluegreen-frontend-preview
  namespace: bluegreen-demo
spec:
  type: NodePort
  selector:
    app: bluegreen-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30081
    name: http